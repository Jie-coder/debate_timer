# Changelog

## 2026-06-29

- 从原始单 HTML 拆分为 `index.html`、`style.css`、`app.js`。
- 新增 `scripts/build-single.js`，可重新生成单文件 HTML。
- 新增 `scripts/check.js`，用于无依赖健康检查。
- 新增首页封面环节。
- 删除环形显示功能。
- 删除计时器大小调节功能。
- 双计时器恢复只显示「正方 / 反方」，不显示学校名。
- 旧打包文件保存到 `legacy/bundled-legacy.html`。
